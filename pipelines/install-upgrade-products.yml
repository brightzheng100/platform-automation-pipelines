#! ----------------------------------------------------
#! master pipeline file for `install-upgrade-products`
#! ----------------------------------------------------

#@ load("groups.lib.yml", "groups_for_install_upgrade_products")
#@ load("resource-types.lib.yml", "resource_types")
#@ load("resources-install-upgrade.lib.yml", "resources")
#@ load("jobs-install-upgrade.lib.yml", "jobs")

---

#! groups
groups: #@ groups_for_install_upgrade_products()

#! resource_types
resource_types: #@ resource_types()

#! resources
resources: #@ resources()

#! jobs
jobs: #@ jobs()
